import DXT from "./js/docxtemplater";

type Filter = (input: any, ...params: any[]) => any;

interface Parser {
  (tag: string): DXT.Parser;
  filters: { [x: string]: Filter };
}

declare const angularParser: Parser;

export { angularParser };
