"use strict";

const DocUtils = require("docxtemplater").DocUtils;

// Convert pixels to emus using the same formula as before
const convertPixelsToEmus = pixel => Math.round(pixel * 9525);

// Set the converted value to DocUtils
DocUtils.convertPixelsToEmus = convertPixelsToEmus;

module.exports = DocUtils;
