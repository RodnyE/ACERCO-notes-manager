"use strict";

const { DocUtils } = require("docxtemplater");

// Convert pixels to emus using a more accurate conversion factor
DocUtils.convertPixelsToEmus = function (pixel) {
  return Math.round(pixel * 9144); // 96 DPI (dots per inch) x 95.254 (conversion factor for emus to inches)
};

module.exports = DocUtils;

