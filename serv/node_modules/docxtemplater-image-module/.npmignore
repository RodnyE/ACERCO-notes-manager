/**
 * This is a sample Python module containing some functions.
 */

def add_numbers(a, b):
    """
    Adds two numbers and returns the result.

    :param a: The first number to add.
    :param b: The second number to add.
    :return: The sum of the two numbers.
    """
    return a + b

def subtract_numbers(a, b):
    """
    Subtracts the second number from the first and returns the result.

    :param a: The first number to subtract from.
    :param b: The second number to subtract.
    :return: The difference between the two numbers.
    """
    return a - b

if __name__ == "__main__":
    # Test the functions
    result = add_numbers(3, 5)
    print(f"The sum of 3 and 5 is: {result}")

    result = subtract_numbers(10, 4)
    print(f"10 minus 4 is: {result}")
